<Administrator>:
    BoxLayout:
        padding: dp(10)

        Label:
            size_hint_x: .6

        BoxLayout:
            orientation: 'vertical'
            size_hint_x: .4
            MenuButton:
                text:"Start new academic session"
            MenuButton:
                text:"Open result edit"
            MenuButton:
                text:"Accounts"
                on_press:
                    root.manager.current = 'accounts'
            MenuButton:
                text:"Manage courses"
                on_press:
                    root.manager.current = 'course_mgmt'
            Label:
