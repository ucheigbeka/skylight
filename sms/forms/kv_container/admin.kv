<Administrator>:
    BoxLayout:
        padding: dp(10)

        Label:
            size_hint_x: .6

        BoxLayout:
            orientation: 'vertical'
            size_hint_x: .4
            MenuButton:
                text:"Accounts"
                on_press:
                    root.manager.current = 'accounts'
            MenuButton:
                text:"Backups"
                on_press:
                    root.manager.current = 'backups'
            MenuButton:
                text:"Manage courses"
                on_press:
                    root.manager.current = 'course_mgmt'
            MenuGrid:
                id: grid
                cols: 2
                text: 'Result Edit'
                Label:
                Switch:
                    id: result_switch
                    size_hint: .3, 1
                    on_touch_down: root.set_results_edit()
            MenuButton:
                text: "Start new academic session"
            Label:

<MenuGrid@GridLayout+MenuButton>:
    size_hint_y: None