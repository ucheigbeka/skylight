<MainPage>:
    dv: dv
    BoxLayout:
        padding: dp(10)

        FloatLayout:
            size_hint_x: .6
            DataViewer2:
                id: dv
                cols: 3
                center_y: .5
                weightings: [.2, .4, .2]
                headers: ['User', 'Action', 'Time']
                _data: root.logs

        BoxLayout:
            orientation: 'vertical'
            size_hint_x: .4

            MenuButton:
                text:"Course Details"
                on_press:
                    root.manager.current = 'course_details'
            MenuButton:
                text: "Personal Data"
                on_press:
                    root.manager.current = 'personal_info'
            MenuButton:
                text:"Course Registration"
                on_press:
                    root.manager.current = 'course_registration'
            MenuButton:
                text:"Result Entry"
                on_press:
                    root.manager.current = 'result_entry_menu'
            MenuButton:
                text:"Reports"
                on_press:
                    root.manager.current = 'page_reports'
            MenuButton:
                text:"Administrator"
                on_press:
                    root.manager.current = 'admin'

            Label:
